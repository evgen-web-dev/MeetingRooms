# Phase N — <short name>

**Satisfies:** assignment requirement(s) #N, #N
<!-- Every phase maps to at least one numbered requirement.
     If you can't fill this in, question whether the phase should exist. -->

**Goal:** One sentence. What is true at the end that wasn't true at the start.

## Tasks
<!-- Ordered. Each small enough to be one atomic commit with a
     "what changed and why" message (requirement #9). -->
- [ ] 
- [ ] 

## Decisions made in this phase
<!-- Only NEW decisions. Standing decisions live in docs/decisions.md.
     If a decision here contradicts that file, update that file too. -->
- 

## Risks
<!-- Anything that could eat an hour. Name the fallback, not just the risk.
     Bad:  "SignalR might be tricky"
     Good: "Azure SignalR connection may fail from App Service.
            Fallback: self-hosted SignalR to prove the feature, swap later." -->
- 

## Tests
<!-- Be honest. "Manual smoke test via Scalar" is a valid entry.
     Only requirement #6 mandates an automated test. Don't invent work. -->
- 

## Done when
<!-- Verifiable statements about system behaviour, not activities.
     Bad:  "review the booking logic"
     Good: "POST /api/bookings returns 409 with code BOOKING_SLOT_TAKEN
            when the slot is already booked" -->
- [ ] 
- [ ] 

**Deploy after this phase:** yes / no

---

## Outcome
<!-- Filled in AFTER the phase, before starting the next one.
     What actually happened, what deviated from plan, what got deferred.
     This is the section the next session reads to get up to speed. -->