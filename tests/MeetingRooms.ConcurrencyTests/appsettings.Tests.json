{
  // Fixtures, not secrets. These values are used only by the test host against a local test
  // database; the real signing key and administrator live in dotnet user-secrets locally and in
  // App Service application settings in Azure, and never in this repository.
  "ConnectionStrings": {
    // The fallback for a reviewer running `docker compose up -d` at the repository root. Inside
    // the dev container the ConnectionStrings__DefaultConnection environment variable overrides
    // it. No catalog is named on purpose: BookingApiFactory chooses one, so the tests can never
    // be pointed at the development database by accident.
    "DefaultConnection": "Server=localhost,1433;User Id=sa;Password=MeetingRooms_Local_1;TrustServerCertificate=True"
  },
  "Jwt": {
    // 32 bytes of base64. Issuer, audience and lifetime come from the API's own appsettings.json.
    "SigningKey": "dGVzdC1vbmx5LXNpZ25pbmcta2V5LWZvci14dW5pdC0wMQ=="
  },
  "Seed": {
    "AdminEmail": "admin@meetingrooms.test",
    "AdminPassword": "Test-Admin-1!"
  }
}
